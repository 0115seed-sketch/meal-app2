name: Auto Update School Meal

on:
  schedule:
    # 한국 표준시(KST) 오전 6시 실행 (UTC 기준 21시)
    - cron: '0 21 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-meal:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests Pillow

      - name: Download Korean Font
        run: |
          curl -L -o NanumGothic.ttf https://github.com/google/fonts/raw/main/ofl/nanumgothic/NanumGothic-Regular.ttf

      - name: Run meal update script
        run: python update_meal.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html images/*.png
          git diff-index --quiet HEAD || (git commit -m "Auto-update meal data: $(date -u -d '+9 hours' +'%Y-%m-%d')" && git push)
